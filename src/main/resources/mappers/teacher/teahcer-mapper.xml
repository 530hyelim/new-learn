<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teacher">
	<select id="getTotalStudentNum" resultType="int">
		select count(*) from class_join
		where class_no = #{classNo} and class_role = 'student'
	</select>
	<select id="getAttList" resultType="attendance">
		select user_no, user_name, entry_time, exit_time, att_status
		from attendance join member using (user_no)
		where class_no = #{classNo} and trunc(entry_time) = trunc(sysdate)
	</select>
</mapper>




